/**
 * Note: emotion provides *a lot* of flexibility in how styles are applied
 * to elements.
 * 
 * Core @emotion/css examples: https://www.npmjs.com/package/@emotion/css
 * Separate @emotion/styled API, which looks similar to styled-components:
 * https://www.npmjs.com/package/@emotion/styled
 * 
 * I'll be using @emotion/css for this example.
 */

 import Button from './Button'
 import DangerButton from './DangerButton'

 export {
   Button,
   DangerButton
 }